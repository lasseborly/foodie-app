const initialState = {
    mydata: 0
}

const app = (state = initialState, action) => {
    switch (action.type) {
      case 'ADD_TODO':
        return 
      default:
        return state
    }
  }
  
  export default app